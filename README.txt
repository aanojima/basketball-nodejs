Run 'npm install' at the root of the project directory to install any dependencies

Run 'npm start' to start the application

Go to http://localhost:8000

Use the controls to play around with the simulation.